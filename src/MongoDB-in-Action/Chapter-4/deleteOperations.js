// Delete a single document matching the given route
db.routes.deleteOne({
  "airline.id": 417,
  "src_airport": "MUC",
  "dst_airport": "LAX"
})

// Delete a single document matching the given route
db.routes.deleteOne({
  "airline.id": 417,
  "src_airport": "MUC",
  "dst_airport": "LAX"
})
